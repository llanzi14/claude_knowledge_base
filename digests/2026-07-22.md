# Digest — 2026-07-22

- **Claude Code v2.1.217 released** (2026-07-21) — new subagent self-protection limits directly relevant to Workflow-based/scheduled automation like this KB routine: a **concurrent subagent cap** (default 20, `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`, distinct from the existing 200-total-per-session cap from v2.1.212), and **subagents no longer spawn nested subagents by default** (`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` to opt back in). Fixed **`--max-budget-usd` not actually stopping background subagents** once the budget was hit. Fixed a memory leak retaining full untruncated MCP tool output after truncation. Also: fixed background-session isolation not canonicalizing symlinked working directories; fixed auto-compact not triggering for Opus 4.8 on Bedrock; fixed corporate mTLS/TLS-verify/OAuth-scope/proxy settings being ignored in Claude Desktop; fixed Windows auto-update failures that could leave `claude.exe` missing; new emoji shortcode autocomplete in the prompt input. `[ACTION]` Update to v2.1.217 — check the new 20-concurrent-subagent / no-nesting defaults against any wide or deep Lanzico Workflow fan-out, and note the `--max-budget-usd` fix if that flag is relied on for spend control. [GitHub CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)
- **Managed Agents Memory API — list-behavior change effective today** (announced 2026-07-02, effective 2026-07-22) — the `agent-memory-2026-07-22` beta header (governing `GET /v1/memory_stores/{id}/memories`) auto-migrates onto the older `managed-agents-2026-04-01` header as of today: memory listings now return a stable server-defined order, `depth` only accepts `0`/`1`/omitted, and `path_prefix` must match whole path segments. Only actionable if Lanzico calls the Managed Agents API directly — no evidence of that currently, logged for awareness since it changes behavior automatically today with no code change required to trigger it.
- **Managed Agents catch-up (2026-07-01, not previously logged)** — session event-delta streaming, backward session-list pagination, per-session agent config overrides, vault credential injection-location control, and fuller agent/deployment/deployment-run webhooks. Minor catch-up item; relevant only if building directly on the Managed Agents API.
- No new model releases; Fable 5/Mythos 5 status unchanged (both stable since the July 1 restoration). No new Anthropic blog/social posts found beyond what's already logged through 2026-07-21. No notable new r/ClaudeAI or r/ClaudeCode threads surfaced this cycle — general and site-scoped searches returned nothing dated after 2026-07-21 specific to either subreddit.
- Knowledge base updated: `releases-and-features.md` (v2.1.217, Managed Agents memory-header migration + catch-up items), `tools-and-integrations.md` (subagent concurrency/nesting/budget caps, Managed Agents catch-up), `suggestions-for-lucas.md` (new ACTION item for v2.1.217's subagent caps).
- Slack summary **not due today** — 3-day cadence, last posted 2026-07-20, next due 2026-07-23. Nothing here is material enough to post out-of-cadence; will roll into the 07-23 post.
