# Digest — 2026-07-15

- **Claude Code v2.1.210 released** (2026-07-14, follows yesterday's v2.1.208 entry) — smaller release, several items relevant to this KB routine's own unattended/scheduled execution and to security: fixed the **`ultracode` keyword opt-in firing on non-human-originated input** such as webhook payloads and relayed PR comments (closes a prompt-injection path where external content could trigger expensive multi-agent workflows without a real user asking); **hardened the Agent tool against indirect prompt injection via content a subagent read**; fixed `isolation: 'worktree'` subagents being able to run git-mutating commands against the **main repo checkout** instead of their own isolated worktree (isolation bypass fix); fixed a hook-callback timeout being misreported to the model as a user rejection, which made unattended sessions stop and wait instead of retrying; auto mode's permission classifier now defaults to **Sonnet 5** for external sessions (pinned per-session on first request). Also: live elapsed-time counter on long-running tool calls, startup warning for misconfigured `Write(path)`/`NotebookEdit(path)`/`Glob(path)` permission rules (use `Edit(path)`/`Read(path)` instead), fixed killed background sessions leaving a permanent `git worktree lock`, fixed `claude agents --effort ultracode` silently being dropped, memory writes exceeding `MEMORY.md`'s read limit now error instead of silently truncating. **v2.1.209** (also 2026-07-14) was a narrow fix: `/model` and other dialogs were incorrectly blocked in `claude agents` background sessions — reverted the overly broad guard. `[ACTION]` Update to v2.1.210 — the `ultracode`/webhook-injection fix and Agent-tool prompt-injection hardening are directly relevant to this routine (it runs unattended via GitHub webhook activity and PR-comment triggers) and to any other scheduled Lanzico automation using Workflow/worktree isolation. [Changelog](https://code.claude.com/docs/en/changelog)
- **Claude for Teachers launched** (2026-07-14, not caught in yesterday's digest) — free access to premium Claude tools, teaching skills, and curriculum connections aligned to all 50 US states' standards, for verified US K-12 educators. Data not used for model training; student data covered by a K-12 Data Processing Addendum written for FERPA compliance. Education-market product, no direct Lanzico action. [Chalkbeat](https://www.chalkbeat.org/2026/07/14/anthropic-launches-claude-for-teachers-as-ai-companies-battle-for-classrooms/)
- **Claude Science application deadline is today** (2026-07-15) — reminder, not new: the "AI for Science" grant program (up to 50 projects, up to $30K in credits each) logged 2026-07-02 closes applications today; award notifications by July 31. No Lanzico action unless there's a science-adjacent client project to nominate.
- No new Anthropic model releases, no Fable 5/Mythos 5 status change since 2026-07-01 restoration. No notable new r/ClaudeAI or r/ClaudeCode threads surfaced this cycle — same "no new consensus tips" pattern as the last several days.
- Knowledge base updated: `releases-and-features.md` (v2.1.210, v2.1.209, Claude for Teachers), `suggestions-for-lucas.md` (new ACTION item for v2.1.210's injection-hardening fixes).
- Slack summary **skipped today** — last posted 2026-07-14 (3-day cadence), next due 2026-07-17. Today's changes are incremental (one reliability/security release, one education-market product launch), not a material shift warranting an early post.
